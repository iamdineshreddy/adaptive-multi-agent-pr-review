# Setup

**Status: pending implementation.** Updated during Phase 1 (workspace layout) and
phase-by-phase after that.

Planned contents:

- Prerequisites (Python 3.11+, Node 20+, Docker, Docker Compose)
- Backend local setup: venv, `pip install -e .[dev]`, `.env` template, Alembic upgrade
- Redis / Postgres (docker compose)
- Frontend: `npm install`, `npm run dev`
- LLM provider env config
- GitHub App/installation setup
- Local test script (`make test`, `make lint`)